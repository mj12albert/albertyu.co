server {
  listen 80;
  listen [::]:80;

  root /var/www/html;
  index index.html index.htm;

  server_name next.albertyu.co;

  location / {
    proxy_pass http://nextjs:3000
  }

  location ~ /.well-known/acme-challenge {
    allow all;
    root /var/www/html;
  }
}
